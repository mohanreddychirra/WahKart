export const setOverlayShow = (show) => dispatch => dispatch({
  type: 'SET_OVERLAY_SHOW',
  show
});

export const setNavBarShow = (show) => dispatch => dispatch({
  type: 'SET_NAVBAR_SHOW',
  show
});
